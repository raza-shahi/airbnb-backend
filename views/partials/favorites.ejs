<form action="/store/add-to-favorites" method="POST" class="mt-6">
  <input type="hidden" name="homeId" value="<%= home ? home.id : '' %>">
  <button type="submit"
    class="w-full bg-blue-600 text-white py-2 px-4 rounded hover:bg-blue-700 transition duration-300">
    Add to Favorites
  </button>
</form>